{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may only invoke this app's own timer commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
