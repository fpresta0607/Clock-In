{
  "name": "@clock-in/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @clock-in/shared build && tsc --noEmit && vite build",
    "dev": "vite",
    "tauri": "tauri",
    "test": "pnpm --filter @clock-in/shared build && vitest run",
    "typecheck": "pnpm --filter @clock-in/shared build && tsc --noEmit"
  },
  "dependencies": {
    "@clock-in/shared": "workspace:*",
    "@tauri-apps/api": "^2.8.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "jsdom": "^26.1.0",
    "vite": "^7.1.3"
  }
}
