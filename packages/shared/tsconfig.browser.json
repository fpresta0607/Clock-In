{
  "//": "The browser half of the package: the `./webgl-shader` entry alone. It is a separate project so the DOM lib and the JSX factory it needs stay off `src/index.ts`, which is what apps/api compiles - a DOM type that reached the contracts would type-check here and fail in node.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src/webgl-shader.tsx"]
}
